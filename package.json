{
  "name": "reversegitops-dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync:manifesto": "git submodule update --init --remote external/manifesto",
    "render:og": "node scripts/render-og-image.mjs",
    "prebuild": "npm run sync:manifesto && npm run render:og",
    "predev": "npm run sync:manifesto",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "lucide-astro": "^0.556.0",
    "marked": "^15.0.0",
    "posthog-js": "^1.434.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
